package info.angrynerds.game.robot;

public abstract class PartLookup {
//	public static boolean doesPartBelong(Part part, Robot robot) {
//		switch(robot.getType()) {
//		case TURTLE:
//			if(part.getName().equalsIgnoreCase("WHEELS")) return true;
//			if(part.getName().equalsIgnoreCase("CONTROLLER")) return true;
//			if(part.getName().equalsIgnoreCase("SHELL")) return true;
//		}	// for now, that's all
//		return false;
//	}
}
